var num1=[];
var num2=[];

function compare(num1, num2) {
    if (num1< num2) {
        return "num2 is bigger";
    } else if (num1> num2) {
        return "num1 is bigger";
    } else (num1 === num2) 
         return "both are the same";
    
};
    
compare(num1(2));
compare(num2(4));
